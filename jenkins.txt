pipeline {
agent any
parameters {
        booleanParam(name: 'autoApprove', defaultValue: false, description: 'Automatically run apply after generating plan?')
    } 
 environment {
    MY_CRED = credentials('AzurePrincipal')
    
  }

 stages {
    stage('build') {
      steps {
          sh 'az login --service-principal -u $Subscription ID -p $Client Secret -t $Tenant ID'
      }
    }
  }
}

